// Stable component boundary. Runtime synchronization and task actions live in
// the controller; plan/review surfaces remain independent presenters.
export { default } from './SynonBiomedRuntimeOperationsController';
