#!/bin/sh
set -eu

case "${SYNON_HOST_PICKER_FIXTURE_MODE:-select}" in
  select)
    printf '%s\n' "${SYNON_HOST_PICKER_FIXTURE_PATH:?fixture path is required}"
    ;;
  cancel)
    exit 1
    ;;
  error)
    printf '%s\n' 'fixture picker failed' >&2
    exit 1
    ;;
  *)
    exit 64
    ;;
esac
